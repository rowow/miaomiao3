import Vue from 'vue'
import Vuex from 'vuex'

// 关于city中的状态管理
import City from './city'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    City
  }
})
